Session Storage
=============

window.sessionStorage

key / value store
both key and value should be strings

APIs
    -setItem(key,value)
    -getItem(key) // => value
    -removeItem(key)
    -key(index) //=> key
    -length
    -clear()
